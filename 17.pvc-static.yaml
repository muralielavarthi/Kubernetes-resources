apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc-static
spec:
  resources:
    requests:
      storage: 5Gi # The amount of storage requested
  volumeName: my-pv-static # bind to the specific PV named "my-pv-static"
